{
  "name": "ai-playwright",
  "version": "0.0.1",
  "description": "AI-assisted utilities for Playwright scripts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@playwright/test": "^1.40.0"
  },
  "peerDependencies": {
    "@playwright/test": "^1.40.0"
  }
}