{
  "name": "ai-wright",
  "version": "0.0.10",
  "description": "AI-steps in your Playwright scripts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "@google/generative-ai": "^0.11.0",
    "axios": "^1.6.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@playwright/test": "^1.40.0"
  },
  "peerDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
